/**
 * Standalone script: Generate the full analysis report.
 * This is equivalent to running the main index.ts.
 * Usage: CODEX_API_KEY=<key> npx tsx src/scripts/generate-report.ts
 */

// Just re-export the main entry point
import "../index.js";
